# ----------------------------------------
# 使用国内的镜像仓库, 降低拉取第三方 crate 的耗时
# ----------------------------------------
[source.crates-io]
replace-with = "ustc"

[source.ustc]
registry = "git://mirrors.ustc.edu.cn/crates.io-index"
